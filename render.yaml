services:
  - type: web
    name: catbox-uploader
    env: node
    buildCommand: curl -fsSL https://bun.sh/install | bash && ~/.bun/bin/bun install && ~/.bun/bin/bun run build
    startCommand: ~/.bun/bin/bun index.ts
    envVars:
      - key: PORT
        value: 10000
    autoDeploy: true
